# Semantic Working Memory Specification

## Overview

The middleware constructs:
temporary semantic working memory.

Working memory is distinct from:
long-term semantic memory.

Working memory contains:

* compressed context
* active semantic relevance
* retrieval intelligence
* orchestration state

The middleware transforms:
long-term memory
into:
temporary actionable semantic context.

---

# Core Philosophy

The middleware distinguishes between:

* stored memory
* retrieved memory
* working memory

Working memory is:
temporary semantic intelligence context.

The middleware prioritizes:
high-density working memory construction.

---

# Working Memory Lifecycle

```txt id="wm1"
Long-Term Memory
↓
Retrieval
↓
Reranking
↓
Compression
↓
Semantic Merging
↓
Working Memory
↓
Generation
```

---

# Working Memory Objectives

Working memory attempts to:

* maximize semantic relevance
* minimize token usage
* preserve relationships
* preserve retrieval confidence

Working memory is:
token-aware and orchestration-aware.

---

# Context Window Philosophy

The middleware does not treat:
model context windows
as infinite memory.

The middleware constructs:
optimized semantic working memory.

The middleware values:
precision,
density,
and semantic clarity.

---

# Final Principle

Working memory is:
temporary semantic intelligence assembly.

The middleware transforms:
stored semantic infrastructure
into:
high-density actionable context.
