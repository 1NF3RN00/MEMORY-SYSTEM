# Token Accounting Specification

## Overview

The middleware continuously tracks:
token economics across providers.

Token accounting evaluates:

* retrieval tokens
* generation tokens
* embedding tokens
* orchestration overhead
* retry costs

Token accounting supports:
cost intelligence infrastructure.

---

# Core Philosophy

Tokens are:
infrastructure resources.

The middleware continuously evaluates:
semantic value per token.

---

# Token Metrics

Examples:

* workflow token usage
* provider token usage
* compression savings
* retry overhead
* generation efficiency

Token accounting remains:
observable and tenant-aware.

---

# Token Philosophy

The middleware prioritizes:
high semantic efficiency per token consumed.
