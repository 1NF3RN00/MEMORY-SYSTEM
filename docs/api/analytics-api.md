# Analytics API Specification

## Overview

The analytics API exposes:
middleware observability infrastructure.

Analytics APIs support:

* retrieval analytics
* token analytics
* orchestration analytics
* graph analytics
* memory analytics

The middleware treats:
observability as infrastructure.

---

# Example Endpoints

```http id="aa1"
GET /v1/analytics/retrieval
GET /v1/analytics/tokens
GET /v1/analytics/workflows
```

---

# Analytics Philosophy

Analytics APIs expose:
semantic infrastructure telemetry.

The middleware values:
observable intelligence infrastructure.
