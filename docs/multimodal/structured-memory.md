# Structured Memory Specification

## Overview

The middleware supports:
structured semantic memory.

Structured memory includes:

* CRM records
* database rows
* APIs
* analytics records
* workflow states
* event streams

Structured memory integrates into:
semantic retrieval infrastructure.

---

# Core Philosophy

Semantic infrastructure should support:
both structured and unstructured information.

The middleware unifies:
structured semantic retrieval.

---

# Structured Metadata

Examples:

* schema version
* field mappings
* source system
* synchronization timestamps

Structured memory remains:
governed and traceable.

---

# Structured Philosophy

Structured data is:
semantic infrastructure,
not merely storage infrastructure.
