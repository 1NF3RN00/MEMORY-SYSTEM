# SDK Pattern Specification

## Overview

SDKs support:
consistent semantic infrastructure integration patterns.

Patterns support:

* retrieval workflows
* ingestion workflows
* graph workflows
* orchestration workflows

The middleware promotes:
predictable integration architecture.

---

# Supported Patterns

Examples:

* retrieval-first pattern
* workflow-execution pattern
* ingestion pipeline pattern
* analytics consumption pattern

SDK patterns improve:
integration consistency.

---

# Pattern Philosophy

SDKs expose:
middleware capabilities,
not hidden orchestration behavior.

The middleware prioritizes:
transparent infrastructure integration.

---

# Final Principle

SDKs are:
developer infrastructure tools.

The middleware remains:
the centralized semantic intelligence platform.
