{
  "name": "@memory-middleware/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Worker scaffold — no processing logic in Sprint 0",
  "scripts": {
    "build": "node -e \"console.log('worker runs via @memory-middleware/api dev:worker')\"",
    "dev": "npm run dev:worker -w @memory-middleware/api",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
