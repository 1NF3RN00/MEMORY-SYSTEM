{
  "name": "@memory-middleware/ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sprint 1 — deterministic ingestion pipeline",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@memory-middleware/normalization": "*",
    "@memory-middleware/observability": "*",
    "@memory-middleware/shared-types": "*",
    "@memory-middleware/structural": "*",
    "cheerio": "^1.0.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/turndown": "^5.0.5",
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
