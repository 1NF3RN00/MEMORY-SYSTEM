{
  "name": "@memory-middleware/observation-providers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Observation collectors — website, PageSpeed, and Apify providers",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@memory-middleware/ingestion": "*",
    "@memory-middleware/observation-ingestion": "*",
    "@memory-middleware/observation-registry": "*",
    "@memory-middleware/shared-types": "*",
    "apify-client": "^2.12.0",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
