{
  "name": "@memory-middleware/context-delivery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Context delivery + rendering layer — transforms middleware state into LLM inference context",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@memory-middleware/domain-engine": "*",
    "@memory-middleware/observability": "*",
    "@memory-middleware/shared-types": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
