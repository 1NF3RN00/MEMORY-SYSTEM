{
  "name": "@memory-middleware/observation-ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Store validated observations as memory objects via ingestion pipeline",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@memory-middleware/ingestion": "*",
    "@memory-middleware/observability": "*",
    "@memory-middleware/observation-registry": "*",
    "@memory-middleware/shared-types": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^6.0.3"
  }
}
